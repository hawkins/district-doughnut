# If I could manage to link OpenSSL successfully using musl,
# then this would be uncommented. However, I've resorted to
# compiling and linking via Docker to subvert the whole thing.
#[target.x86_64-unknown-linux-musl]
#linker = "/usr/local/bin/x86_64-linux-musl-gcc"
